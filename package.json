{
  "name": "wedding",
  "private": true,
  "scripts": {
    "local:setup": "node scripts/local.cjs setup",
    "local:install": "node scripts/local.cjs install",
    "local:db": "node scripts/local.cjs db",
    "local:migrate": "node scripts/local.cjs migrate",
    "local:seed": "node scripts/local.cjs seed",
    "local:status": "node scripts/local.cjs status",
    "api": "npm run start:dev --prefix apps/api",
    "web": "npm run dev --prefix apps/web"
  }
}
